{
  "name": "corvus-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules tests run against the Firestore emulator",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-corvus \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
