# Vercel Python function dependencies for api/main.py.
#
# This file previously excluded mediapipe/opencv on the assumption that
# Vercel's Python function size limit was 250MB (with real posture
# analysis falling back to rule-based scoring). That limit is now
# 500MB standard / 5GB with the Large Functions beta enabled — the real
# analysis engine fits, so it's no longer excluded. Rather than
# maintain a second, different dependency list here, just include the
# one real list (see requirements.txt at the repo root and
# backend/requirements.txt, which it in turn includes).
-r ../requirements.txt
